# Node
node_modules/

# Signing key — NEVER commit. The catalog is signed by the Hillnote website
# backend (its CATALOG_SIGNING_KEY), not in this repo. Only public-key.json is tracked.
keys/ed25519-private.pem
*.pem

# Local tooling / IDE — never publish
.claude/
.idea/

# OS
.DS_Store
